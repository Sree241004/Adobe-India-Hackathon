{
  "type": "object",
  "properties": {
    "title": {
      "type": "string",
      "description": "The title of the PDF document"
    },
    "outline": {
      "type": "array",
      "description": "List of headings extracted from the document",
      "items": {
        "type": "object",
        "properties": {
          "level": {
            "type": "string",
            "enum": ["H1", "H2", "H3", "H4"],
            "description": "Heading level based on font size or document structure"
          },
          "text": {
            "type": "string",
            "description": "The actual text of the heading"
          },
          "page": {
            "type": "integer",
            "minimum": 0,
            "description": "Page number where the heading appears"
          }
        },
        "required": ["level", "text", "page"]
      }
    }
  },
  "required": ["title", "outline"]
}
